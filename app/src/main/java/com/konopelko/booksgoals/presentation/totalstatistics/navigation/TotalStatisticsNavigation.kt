package com.konopelko.booksgoals.presentation.totalstatistics.navigation

import androidx.navigation.NavController
import com.konopelko.booksgoals.presentation.totalstatistics.TotalStatisticsIntent.TotalStatisticsNavigationIntent

class TotalStatisticsNavigation(
    private val navController: NavController
) {

    fun onNavigate(intent: TotalStatisticsNavigationIntent) = when(intent) {
        else -> {}
    }
}